name: "Composite Action"
description: "[Test] Composite Action"

runs:
  using: "composite"
  steps:
    - name: "Checkout"
      shell: bash
      run: |
        ::group::step1
        echo 'step1'
        ::endgroup::

    - name: "Hello World"
      shell: bash
      run: |
        ::group::step2
        echo 'step2'
        ::endgroup::
name: "Composite Action"
description: "[Test] Composite Action"

runs:
  using: "composite"
  steps:
    - name: "Checkout"
      shell: bash
      run: |
        echo "::group::step1"
        echo "step1の内容"
        echo "::endgroup::"

    - name: "Hello World"
      shell: bash
      run: |
        echo "::group::step2"
        echo "step2の内容"
        echo "::endgroup::"
name: "Composite Action"
description: "[Test] Composite Action"

runs:
  using: "composite"
  steps:
    - name: "Checkout"
      uses: |
        ::group::checkout
        actions/checkout@v2
        ::endgroup::

    - name: "Hello World"
      shell: bash
      run: |
        ::group::HelloWorld
        echo 'Hello World'
        ::endgroup::